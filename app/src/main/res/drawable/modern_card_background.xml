<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Soft outer shadow for 3D depth -->
    <item android:top="6dp" android:left="4dp" android:right="4dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <solid android:color="#33000000"/>
            <corners android:radius="16dp"/>
        </shape>
    </item>

    <!-- Main elevated surface with dark blue gradient and subtle border -->
    <item android:top="0dp" android:left="0dp" android:right="6dp" android:bottom="6dp">
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="135"
                android:startColor="@color/secodary"    
                android:endColor="@color/secodary2"/>
            <corners android:radius="16dp"/>
            <stroke
                android:width="1dp"
                android:color="@color/divider_color"/>
        </shape>
    </item>

</layer-list>
